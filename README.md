Setup Instructions:

Everything has been containerized, first make sure you have docker installed then simply run
docker compose up -d --build

Drupal will be on localhost:8080
The Symfony app will be on localhost:8081

